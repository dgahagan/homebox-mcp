{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.dgahagan/homebox-mcp",
  "description": "Homebox home-inventory server: search items, intake with manuals/receipts, warranties, attachments, locations, tags, and QR labels",
  "repository": {
    "url": "https://github.com/dgahagan/homebox-mcp",
    "source": "github"
  },
  "version": "0.9.1",
  "packages": [
    {
      "registryType": "pypi",
      "identifier": "homebox-mcp",
      "version": "0.9.1",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "HOMEBOX_URL",
          "description": "Base URL of your Homebox instance (>= 0.26), e.g. https://homebox.example.com",
          "isRequired": true,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "HOMEBOX_TOKEN",
          "description": "Homebox API key (Profile -> API Keys, starts with hb_)",
          "isRequired": true,
          "format": "string",
          "isSecret": true
        },
        {
          "name": "HOMEBOX_ALIAS_FIELD",
          "description": "Optional: name of one custom field treated as a stable item identifier for lookups",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        },
        {
          "name": "HOMEBOX_LABEL_DIR",
          "description": "Optional: directory where label/QR PNGs are saved (default: current directory)",
          "isRequired": false,
          "format": "string",
          "isSecret": false
        }
      ]
    }
  ]
}
